const [a, b, c, d] = [3, 15, 5, 9]

const soma = a + b + c + d
const subtracao = b - a
const multiplicacao = c * d
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)